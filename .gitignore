config.toml
.env
venv/
__pycache__/
*.pyc
*.pyo
.mypy_cache/
.ruff_cache/
.pytest_cache/
logs/
/state/
/receipts/
*.egg-info/
dist/
.DS_Store

# Real receipts pulled from the mailbox for local verification — never commit
# these: this repo is public and they are personal financial records.
tests/fixtures/pdf/real_*.pdf

# Real transfer-verdict corpus for scripts/validate_transfer_verdicts.py — never
# commit: real account names, amounts and bank descriptors. Ship only the
# synthetic tests/fixtures/transfer_corpus.example.json.
tests/fixtures/transfer_corpus.real.json
